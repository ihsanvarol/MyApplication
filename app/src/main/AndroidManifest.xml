<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.eliva.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_010_psychology_symbol"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_010_psychology_symbol"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".NotesActivity"
            android:label="@string/title_activity_notes">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.eliva.myapplication.MainActivity" />
        </activity>
        <activity
            android:name=".ClientsActivity"
            android:label="@string/title_activity_bottom2"
            android:parentActivityName=".MainActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.eliva.myapplication.MainActivity" />
        </activity>
        <activity
            android:name=".BottomActivity"
            android:label="@string/title_activity_bottom" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>